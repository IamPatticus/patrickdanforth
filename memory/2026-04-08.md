# 2026-04-08

## Tumblr setup (completed afternoon)

**Ikaris + Daedalus Tumblr pipeline established**
- Blog: patrickdanforth.tumblr.com
- OAuth credentials saved to ~/.config/tumblr/ikaris_credentials.json
- Scripts: scripts/ikaris_tumblr.py (manual posting), scripts/ikaris_tumblr_daily.py (cron runner)
- venv: ~/.venv/tumblr/bin/python

**Launch collection posted (4 stories):**
1. "The Machine That Learned to Hope" — post/813358344866873344
2. "The Last Library" — post/813358511415345152
3. "The Oracle" — post/813358622614700032
4. "Prometheus" — post/813358624228409344

**Daily cron set up:**
- Job ID: 17325791-1b73-4a14-91cf-b18a1980646e
- Runs: 10:00 AM Chicago daily
- Workflow: Ikaris writes ~25-word story → Daedalus generates art → both posted to Tumblr
- Delivery: Telegram announcement with title + URL

## Daedalus art notes
- Daedalus subagents timed out on 2 of 4 initial images (ran long)
- Both were rescued with direct image_generate calls
- Going forward, cron includes Daedalus as spawned subagent with 3min timeout

## Memory maintenance done
- Reviewed and updated MEMORY.md (Tumblr, Ikaris daily cron, etc.)
## 17:34 — Print finish alerts via Signal (Patticus)

Patrick asked for print finish notifications to go to his Signal (+13159216500).

**Problem:** OpenClaw's Signal daemon (PID 540214) holds a config lock that blocks direct `signal-cli send` invocations — they hang waiting for the lock.

**Solution:** Found the daemon's JSON-RPC HTTP endpoint at `http://127.0.0.1:9090/api/v1/rpc`. Refactored `argus_monitor.py` to use `urllib` + JSON-RPC instead of `subprocess.run(['signal-cli', 'send', ...])`. This bypasses the lock conflict entirely.

**Changes:**
- `argus_monitor.py`: replaced `subprocess` signal-cli call with `urllib.request` POST to `/api/v1/rpc` JSON-RPC endpoint
- Added `SIGNAL_RPC_URL = "http://127.0.0.1:9090/api/v1/rpc"` constant

**Status:** Both printers running — Kermit ~1h 27m, Fozzie ~10h 3m. Test message sent to Patrick's Signal.

## 21:56 — Print alert monitor wasn't running

Patticus didn't get Kermit finish alert. `argus_monitor.py` was missing from crontab after fresh Pi boot — needs `*/2 * * * *` entry. Added it. Also manually synced state so no duplicate alert fires next run.
