# 2026-05-05 Daily Log

## OpenClaw Upgrade: 2026.5.2 → 2026.5.4
- Fought for ~3 hours against SIGTERM/circular dependency — `openclaw update` kills the gateway which kills the session
- Ultimately won via system crontab: script killed PID directly, `npm install -g openclaw@2026.5.4`, started gateway manually
- Post-upgrade cleanup: removed stale `openclaw-web-search` extension + config entries, cleaned `ollama_web_*` tool aliases, installed `@openclaw/discord` external plugin
- All channels healthy: Telegram, Signal, Discord

## Shelly Garage Door — HA Automations
- Re-created YAML automations for `automations.yaml` (HA uses `!include automations.yaml`)
- Auto-close at 10pm, auto-open at 7am — both passed config check
- Patticus installed a physical bypass button for the security+ system

## ComfyUI + Daedalus Bridge
- Patticus had set up ComfyUI (Flux dev) on the Strix RTX 5070 last night
- Built `services/comfyui_bridge.py` — REST client for ComfyUI API
- Built `scripts/daedalus_comfyui.py` — Daedalus art pipeline wrapper
- ComfyUI reachable briefly (health check passed), then went dark — likely Windows Firewall blocking sustained HTTP
- **TODO (2026-05-06):** Add Windows Firewall inbound rule for port 8188, retest generation

## Other
- Patticus built a log fence (manual labor!) while I fought the upgrade
- Chinese bug surfaced again with DeepSeek V4 — brief slip, no action taken
