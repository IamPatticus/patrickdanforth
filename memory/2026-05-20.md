# 2026-05-20 — Daily Log

## Reginald Daily Cartoon
- Fixed syntax error in `reginald_daily.py` — line 264 had `with open(fb_path, 'wb')` misindented outside the try block
- Brightness boosted from 50% → 100% in both daily and boot scripts
- Updated quote pool — Reginald now knows the AC is here (no more "AC is coming" whining)
- Set up NVMe archive at `/mnt/nvme/images/reginald-daily/archive/` — auto-copies every generation
- Reginald now shows on screen with full brightness and fresh art daily

## Pi Connect Debugging
- Root cause chain discovered:
  1. Pi boots to console → no Wayland compositor → no wayvnc
  2. The prior night's fix used `vc4-kms-v3d` + virtual HDMI, but `/home/patrick/.config/labwc/environment` had `WLR_BACKENDS=headless` hardcoded
  3. DRM backend requires seat permissions (`seatd` installed, but labwc can't open VT without root)
  4. WayVNC config was empty (0 bytes), then had RSA key incompatibility with nettle 0.9.x
- Final working setup (survives reboot):
  - rpi-connect-headless-wayland.service: labwc with headless backend, 1280x720 output
  - rpi-connect-background.service: swaybg loading today's Reginald image
  - rpi-connect-wayvnc.service: wayvnc with proper config (TLS certs, no RSA key)
  - rpi-connect.service: rpi-connectd starts last, finds everything ready
  - All services enabled, wired in order with `Wants`/`After` dependencies
- `rpi-connect doctor` passes all checks (STUN, TURN, WebSocket, API, auth)
- Screen sharing still fails to connect from client side despite everything local being healthy
- Patticus gave up for now after 30+ minutes of debugging

## Pironman Fan LED Timer
- Changed from "follow" mode to scheduled ON/OFF
- ON at 8am, OFF at 10pm daily via cron jobs (68d8cb1a, f3b07805)

## xAI / Grok Onboarding
- xAI API key loaded as env var (XAI_API_KEY)
- Primary model temporarily set to xai/grok-4.3
- Patticus decided to keep ollama/deepseek-v4-pro:cloud as primary until xAI credits purchased
- Reverted primary back to DeepSeek V4 Pro
- Grok 4.3 ready for use when credits are loaded

## Memory Curation Cron Fix
- Twice-weekly MEMORY.md auto-curation was failing since 2026.5.12 upgrade
- Root cause: "Requested agent harness codex is not registered" — codex harness no longer exists
- Fixed: set sessionTarget to "isolated" (native), removed harness reference, updated to use GPT-5.4 with 600s timeout

## Parallel Victron Multiplus II — Planning Discussion
- Patticus plans to add a second Victron Multiplus II 48/5000 in parallel
- Reason: 3× AC units (house mini-split + 2× EG4 hybrid window units) all pull from solar + battery, risk exceeding ~3000-4000W ceiling especially in summer derating
- 5× Lifepower4 stack (~25.6 kWh) is sufficient for battery — capacity not the issue, inverter headroom is
- Parallel config preferred over single larger inverter: cheaper ($1100-1300 vs $2500+), redundancy, easier install
- Critical wiring details discussed:
  - DC cables (battery bus → each Multiplus) MUST be identical length, gauge, routing for equal load sharing
  - AC cables should match but less critical
  - Both units share ONE shunt (no second shunt needed)
  - VE.Bus comms cable between units — standard Ethernet patch cable, no length parity required
  - Each inverter gets its own DC fuse
  - Both must be identical model + firmware
- Subpanel needed: main breaker panel is full; 6-space subpanel fed from main via 60A dual-pole to free up slots for new inverter breakers
- Current DC run: 1ft positive shunt→breaker + 1ft breaker→inverter (2ft total), will need to be replaced with matched pairs when second unit added
- Patticus will do the install himself; planning phase now — mapping, parts list, then build
- Patticus's approach: "head needs to wrap the brain around it first" — plan, parts, build sequence
- **F3800 purchased at $1,999** (later received $200 back, final price **$1,800**) — will connect via Chargeverter to top up EG4 stack, can also run AC standalone, expandable to 53.8 kWh
