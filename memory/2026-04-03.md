# 2026-04-03 — Talos Hardening + Vector Memory

## Session: Patticus + Talos

## Work Done

### OpenClaw Update 2026.4.2
- Updated from 2026.3.31 → 2026.4.2
- Gateway restarted cleanly (pid 204193)

### Security Hardening (applied)
- `plugins.allow` set to `["openclaw-web-search"]` — fixes critical security warning
- `gateway.auth.rateLimit` added: `maxAttempts: 10, windowMs: 60000, lockoutMs: 300000`
- `gateway.controlUi.allowInsecureAuth` set to `false`
- `gateway.nodes.denyCommands` fixed to use exact command names (`canvas.*` etc.)

### Cron Job Fix
- Both cron jobs (Morning Report + Twice-Weekly MEMORY Curation) were failing
- Error: "Channel is required when multiple channels configured"
- Fixed by adding explicit `delivery.channel: "telegram"` to both jobs

### Vector Memory (fixed)
- Previously paused — needed GEMINI_API_KEY
- Pulled `nomic-embed-text` (~274MB) into Ollama
- Updated `memory_flush.py` to use Ollama `/api/embeddings` (local, no external calls)
- Flushed memory files — 10 chunks stored (5 from 2026-04-01.md, 5 from MEMORY.md)
- Updated HEARTBEAT.md and MEMORY.md to reflect change

## Notes
- MetaClaw discussed — Patticus wanted memory layer, decided against it for now
- Ollama embeddings are a good local alternative to Gemini
- Remaining SSH hardening items deferred (fail2ban, unattended-upgrades, UFW) — noted for future

## Follow-ups
- SSH hardening: fail2ban, unattended-upgrades, UFW (down the road)
- MetaClaw: revisit later if wanted
