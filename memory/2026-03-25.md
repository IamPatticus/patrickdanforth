# 2026-03-25

## Summary
- Patticus approved building out the team roster for Argus, Forgekeeper, and Grange.

## Conversations
- Patticus liked the choices for the expanding crew: Argus, Forgekeeper, and Grange.
- We defined them as a balanced set under Talos: watcher, builder, and steward.

## Decisions
- Added a new team roster document for the crew.
- Established the core conceptual split:
  - Argus = perception
  - Forgekeeper = creation
  - Grange = stewardship

## Projects
- Team/worldbuilding for Talos and the broader roster identity.

## Progress
- Expanded `team/ROSTER.md` with full bios, responsibilities, practical ecosystem mapping, visual language, and sigil concepts for Talos, Argus, Forgekeeper, and Grange.
- Created `team/VISUAL_PACK.md` with shared house style, color palettes, avatar prompts, sigil concepts, UI styling cues, and a house sigil prompt for the crew.
- Generated portraits for Argus, Forgekeeper, Grange, and Talos, plus a house sigil.
- Created `team/CREW_BOARD.md` to unify the roster, offices, phrases, functions, and artifact list into a single crew-facing document.
- Drafted `team/SANCTUM_DASHBOARD_SPEC.md` to map the four-character household into a useful dashboard card system with real operational data.
- Built `team/SANCTUM_BOARD_CONCEPT.md` as a static first-pass sanctum board with house status, four crew cards, metrics, and next-step hooks.
- Folded a new "Sanctum Offices" panel into `services/mission-control/index.html`, adding live-styled cards for Talos, Argus, Forgekeeper, and Grange without disrupting the existing dashboard structure.
- Repositioned the Sanctum Offices panel into the left column for breathing room and added first-pass action links/anchors so the cards can jump to relevant dashboard sections.
- Strengthened the Sanctum Offices UI in `services/mission-control/index.html` with a featured Talos card, office badge styling, richer metrics, and a light dynamic Argus watch update tied to printer state already on the page.
- Restructured the top of `services/mission-control/index.html` so the Print Farm Watch panel sits at the top-right beside the Command Deck, improving the dashboard’s first-glance layout.

## Notes
- The names and roles feel intentionally mythic, grounded, and useful rather than ornamental.
- The crew now has a clear functional split for real OpenClaw work: Talos guides, Argus watches, Forgekeeper builds, and Grange keeps continuity.
- The visual identity now supports turning the roster into avatars, profile pages, and dashboard styling without starting from scratch.
- The project has moved from naming/worldbuilding into an actual artifact set with reusable docs and generated visuals.
- The dashboard spec deliberately keeps the project operational rather than letting it drift into empty aesthetic roleplay.
- The sanctum board now exists in concept form and can be translated into a published page or live dashboard UI.

## Later Progress
- Fixed duplicate dashboard sections while refining Chaotic Sanctum layout, including duplicate Sanctum Offices and duplicate Print Farm Watch blocks.
- Confirmed the laptop-hosted Ollama instance is reachable from the Pi over both LAN and Tailscale.
- Added OpenClaw alias `local-qwen` for `ollama/qwen2.5:3b`.
- Switched OpenClaw’s Ollama base URL from the laptop LAN IP to the Tailscale hostname `http://asusrog.tail4695cd.ts.net:11434`.
- Verified `openclaw models list` shows `ollama/qwen2.5:3b` with alias `local-qwen`.

## Local LLM / Ollama Notes
- Tested local models on the ASUS ROG laptop and found `qwen2.5:3b` to be the best practical local model so far.
- `phi3:mini` worked but felt slower and not clearly better than `qwen2.5:3b` on that hardware.
- Recommendation for that laptop is to stick with the small, responsive model lane rather than push into heavier models.

## Nerve / Cockpit Progress
- Installed `openclaw-nerve` on the Pi at `/home/patrick/nerve`, built it successfully, and configured it against the local OpenClaw gateway.
- Added Tailscale Serve path routing so Nerve is exposed at `https://serenity.tail4695cd.ts.net/nerve/` alongside `/` and `/mission/`.
- Created and enabled a persistent `nerve.service` systemd unit so the cockpit survives beyond an interactive shell process.
- Fixed Nerve’s subpath deployment issue by patching Vite config to build with `/nerve/` asset paths.
- Observed that initial manual connection inside Nerve failed when using `ws://`; the correct WebSocket endpoint for this setup is `wss://serenity.tail4695cd.ts.net`.
- Nerve may still ask for manual gateway connection details instead of auto-trusting the proxy path; if so, use the secure WebSocket endpoint and the existing OpenClaw gateway token.
