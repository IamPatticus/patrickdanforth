# 2026-04-14 — Headache App v1.2 + Google Sheets Sync

## What We Did

### Firebase App Distribution
- Set up Firebase project `headache-tracker-db8f3`
- Added Android app `com.chaoticsanctum.headachelog` to Firebase
- Downloaded and added `google-services.json` to Android Studio project
- Added Firebase SDK (App Distribution API) to Gradle
- Uploaded APK to Firebase App Distribution for beta testing

### Local-Only Migration (v1.1 → v1.2)
Complete rewrite — removed all network/Retrofit code, replaced with Room database.

**Added:**
- `Episode.kt` — Room entity with epoch millis for start/end
- `EpisodeDao.kt` — Room DAO with Flow-based queries
- `AppDatabase.kt` — Room database singleton
- Updated `HeadacheRepository.kt` — CRUD via Room instead of API
- Updated `HeadacheViewModel.kt` — AndroidViewModel + Room Flow observation
- Updated `HeadacheScreen.kt` — uses Episode entity (epoch ms instead of ISO strings)

**Removed:**
- `NetworkModule.kt`, `ApiConfig.kt`, `HeadacheApi.kt` (all network code gone)
- Retrofit, OkHttp, Gson dependencies

### Google Sheets Sync
- Enabled Google Sheets API + Google Drive API in Google Cloud Console
- Created OAuth client ID for Android (SHA-1: `B0:E7:A3:07:E2:C9:44:F7:49:A8:1D:36:91:01:A8:4A:96:0C:0B:D4`)
- Created `SheetsSyncManager.kt` — handles Google Sign-In, spreadsheet creation/lookup, row appending
- Updated `MainActivity.kt` — Google Sign-In activity result handling
- Updated `HeadacheViewModel.kt` — `syncToSheets()` method, auto-syncs on `logStop()`
- Updated `HeadacheScreen.kt` — added "Sync to Sheets" button + status line
- Added Google API client libraries to Gradle
- Bumped Kotlin from 1.9.20 → 2.1.0 (required by Google API libs)
- Bumped KSP from 1.9.20-1.0.14 → 2.1.0-1.0.29
- Bumped Compose compiler from 1.5.4 → 1.5.14

### Icon
- Tried adaptive icon with black background — Patticus preferred the original brain_icon.png
- Reverted to `@drawable/brain_icon` in AndroidManifest

## Issues Resolved
- Kotlin metadata version mismatch (binary 2.1.0 vs expected 1.9.0) → bumped Kotlin to 2.1.0
- SHA-1 not available via keytool (debug keystore missing) → used apksigner from Android SDK
- Firebase App Distribution email invite "must open in email app" → fixed by having tester open invite on phone, use Chrome as default browser
- OAuth 403 error → fixed by adding test user email to OAuth consent screen in Google Cloud Console

## What's Working
- Headache Log app runs fully offline on the phone (Room database)
- Firebase App Distribution for beta testing
- Google Sheets sync — each user's episodes go to their own "Headache Log" spreadsheet in their Drive
- Export CSV still functional

## Future Ideas
- Google Play Store listing ($25 developer fee, needs privacy policy page + screenshots + AAB build)
- Manual sync trigger for active headaches (currently only syncs on END)
- Maybe: notification reminders if a headache has been active too long