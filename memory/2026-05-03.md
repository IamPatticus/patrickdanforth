# 2026-05-03 Daily Log

## Session Summary

### Infrastructure Fixes
- **DeepSeek V4 Pro 503 errors:** Fixed by adding fallback models (GPT-5.4, DeepSeek V4 Flash). Telegram was getting "AI too busy" because the model had zero retry/fallback configured.
- **Whisplay-IM crash loop:** Channel in auto-restart 8/10 — plugin SDK not found at Homebrew paths. Disabled the channel to stop resource drain.
- **Google Drive sync reports:** Patticus asked to stop Telegram notifications for every sync run. Disabled.
- **Signal image handling:** Signal was failing when users sent images — DeepSeek models are text-only and GPT-5.4 hit billing quota. Pulled and configured **moondream** (~1.7GB lightweight vision model) as `agents.defaults.imageModel`. Verified working at 18:55.
- **OpenClaw upgrade:** Upgraded to **2026.5.2**. Cleaned up stale `plugins.allow` entries (discord, telegram, signal — none matched). Ran doctor for PATH warnings.
- **Model fallback chain confirmed:** DeepSeek V4 Pro → GPT-5.4 → DeepSeek V4 Flash. Image model: moondream.

### Configuration State
- `agents.defaults.model.primary`: ollama/deepseek-v4-pro:cloud
- `agents.defaults.imageModel`: ollama/moondream:latest
- Fallback models: openai/gpt-5.4, ollama/deepseek-v4-flash:cloud
- Gateway restarted multiple times during fixes — all channels healthy after restart.

### Avatar
- Talos avatar: avatars/talos-avatar-v4.png
- Patticus avatar: avatars/patticus-avatar.png (confirmed present)
