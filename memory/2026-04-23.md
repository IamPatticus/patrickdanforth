# 2026-04-23

- Google Play upload work for the Headache Log Android app moved to an x86_64 desktop because the Pi cannot reliably build against Android API 35 with its current ARM/qemu/aapt2 setup.
- For the current Play listing, the Android app must keep `applicationId = "com.chaoticsanctum.app"`; changing it creates a different app and breaks the existing listing.
- The stable Android v2 config for the current Play push is: `namespace = "com.chaoticsanctum.headachev2"`, `applicationId = "com.chaoticsanctum.app"`, `compileSdk = 35`, `targetSdk = 35`, `versionCode = 5`, `versionName = "2.0.3"`.
- Google Play rejected one upload because `versionCode 4` had already been used; the next release was bumped to `versionCode 5`.
- `google-services.json` needed to be fixed/replaced to match `com.chaoticsanctum.app` after package-id cleanup on the desktop build.
- A Play Store feature graphic was created at `/home/patrick/.openclaw/workspace/headache-log-feature-graphic.png` (1024x500 PNG).


## End of day memory maintenance

### Headache Log Play submission — evening session
- User submitted the wrong app build to Google Play (was building from an older project directory, not `/home/patrick/headache-app-v2/`). Catch: the user thought they were on the right build but the console had the older one.
- Caught by reverse-engineering the AAB and checking its actual contents vs expected.
- After correction, the correct v2 AAB was built from `/home/patrick/headache-app-v2/` on desktop.
- A second incorrect build (from the old project) was already in review — user uploaded the corrected AAB via "Upload new release" on the Closed testing track to supersede it.
- Keystore secret rotation: moved signing credentials from hardcoded `app/build.gradle.kts` to a separate `key.properties` file that is gitignored. Pattern now: `key.properties` holds the real secrets, example template in `key.properties.example`.
- Export bug fixed: was sharing plain text (MIME `text/csv`) which Android saved as `.txt`. Fixed to use `CreateDocument` activity result with filename `headache-log.csv` and proper file write via `contentResolver.openOutputStream`.
- Pi v2 project version bumped to `versionCode = 7`, `versionName = "2.0.5"` to account for the export fix and differentiate from the version-6 build that was already submitted.
- User is copying the full `/home/patrick/headache-app-v2/` folder to desktop for the next build.
- Play review still pending at session end.
- Desktop build target: x86_64, Android API 35.
