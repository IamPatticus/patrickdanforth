# 2026-06-16

## Morning

- User woke up early, noticed **Morning Heartbeat cron failed** (kimi-k2.5 model timeout)
- Investigated cron list and found same pattern in **Rockin Regi Weekly Comic**
- **Upgraded ALL crons to kimi-k2.6** with appropriate timeouts to prevent future flakes

## Home Assistant Cleanup (Morning Session)

- Ran HA monitor, found **155 unavailable** entities (messy baseline)
- Investigated and categorized:
  - **Victron**: 65 unavailable (device 224 + 14 orphaned ESS settings)
  - **Spoolman**: 24 unknown (API broken)
  - **Govee**: 20+ (BLE beacons cycling in/out)
  - Media players, device trackers: expected noise

- **Updated ha_monitor.py** with noise filtering (media_player, device_tracker, Govee/RuuviTag)
- **Tuned thresholds** and added state migration to skip false delta alerts
- User removed **Spoolman integration** entirely (not using it)
- User removed **Victron BLE** integration (8 devices, 14 entities)
- The 14 orphaned ESS settings couldn't be deleted via API (401/404)
  - Filtered them in the monitor as noise (orphan list of 14 entity IDs)
- Post-cleanup: **~43 real unavailable** (clean baseline), Victron and Spoolman resolved

## Govee Migration

- User hit Govee cloud API rate limits frequently (devices go offline)
- Recommended **govee2mqtt** (LAN + AWS IoT bridge) vs built-in Govee LAN
- **govee2mqtt advantages**: real-time updates, LAN-first, falls back to Platform API, supports scenes/music/segments
- User found add-on already in add-on store (b9845f46_govee2mqtt)
- Started installation/config

## MQTT Template Errors

- After installs/reboots, seeing template errors in logs:
  - `sensor.battery_temp`: null value → template crashes
  - `sensor.battery_remaining`: null value → template crashes
- Likely from Victron MQTT sensors for dead/removed device 224
- Suggested fixes: add null guards, disable sensors, or restart Mosquitto
- User will tackle tomorrow

## Summary (Morning)

- **Crons**: All stable on k2.6
- **HA Monitor**: Properly tuned, noise-filtered, no false alerts
- **Victron**: 14 orphans filtered (can't delete via API), live devices 226/227 healthy
- **Spoolman**: Gone
- **Govee**: Migrating to govee2mqtt (pending)
- **MQTT Templates**: Need null handling (pending)

---
End of morning session 02:10 UTC

---

## Afternoon — Home Assistant Cleanup Round 2

User asked: "Can you make changes and fixes to Home Assistant?" → Yes, via REST API + websocket.

### Actions Completed:
- **Deleted 2 Victron BLE config entries** (SmartShunt + Front Array)
- **Removed 14 Victron ESS orphan sensors** from entity registry via websocket
- **Deleted 4 dead integrations** (Reolink, Google Home, Wyoming Assist Mic, Nest)
- **Removed 9 ghost "unnamed" Tapo sensors** from deleted Nest
- **Removed 6 Nest smoke alarm buttons** + 3 self-test sensors
- **Removed 2 Synology DSM duplicate buttons** (FalconLabs `_2`)
- **Removed 4 Wyoming voice orphans** (STT, wake word)
- **Removed 2 dead MQTT sensors** (battery_temp, solar_charger_state)
  - Later recreated by retained MQTT topics, cleared via MQTT Actions + re-deleted via websocket
- **Installed websockets** (pip3 install) for entity registry websocket operations

**Results:**
| Metric | Before | After |
|--------|--------|-------|
| Total entities | 1,319 | **1,268** (-51) |
| Unavailable | 170 | **82** |
| Real unavailable | 110 | **44** |
| Unknown | 41 | **26** |

**Kermit back online:** 55/55 entities OK after reboot. Fozzie still needs reboot (9 unknown buttons).

### Still remaining for Round 3:
- Fozzie printer (user's reboot)
- 2 Victron ESS battery-life selectors (firmware/config on GX)
- TP-Link plug `master_bedroom_light_on_since` (offline — check plug)
- Phone notify services (expected when offline)
- Garage door opener ESP (`button.garage_door_opener_restart`, `event.garage_door_opener_input`)
- 4 Govee BLE dead devices (H5074_0cf9, H5074_f285 — user removed from govee2mqtt)
- 2x `update.govee_*` entries (add-on updates)

### Tools Installed:
- `websockets` Python package (pip3 install --break-system-packages)
- HA long-lived access token confirmed working for REST + websocket

### Key Discovery:
- HA websocket requires **incrementing message IDs** — all 14 Victron orphans failed on first attempt with "Identifier values have to increase"
- `config/entity_registry/remove` works but entities may be recreated by retained MQTT topics
- `mqtt.publish` with empty retained payload via **Actions** (formerly Services) clears MQTT retained messages

---

## Reginald Daily Flipbook Fix

User reported flipbook last updated Saturday the 13th. Investigation:

- **Root cause:** Two flipbook directories existed:
  - `services/reginald-flipbook/` — had images through June 16 but pushed to a **deleted/non-existent** GitHub repo (404)
  - `patrickdanforth-site/reginald-flipbook/` — **live site**, only had through June 13
- **Sync script** rebuilt index but never actually pushed to the live site

### Fix:
- Copied missing June 14–16 comics to live site
- Rebuilt `index.json` (29 entries)
- Committed + pushed to `patrickdanforth.com` → live now
- **Rewrote `sync_reginald_flipbook.sh`** to target live site and auto-push to GitHub Pages
- Updated cron job name/description
- Script now commits and pushes automatically if there are changes

---
End of day session
