# 2026-03-31 — IT Manager Day

## Session: Patticus + Talos (web TUI + Control UI)

### What we did

**IT Manager Setup**
- Created `scripts/health_check.py` — monitors gateway, reverse proxy, Ollama, Tailscale, disk
- Created `scripts/daily_digest.py` — generates daily system status report
- Created systemd unit files for health check (every 5 min) and daily digest (08:00)
- Enabled `openclaw-health.timer` and `openclaw-digest.timer`

**Reverse Proxy — WebSocket Support**
- Rewrote `services/reverse_proxy.py` with proper WebSocket tunnel support
- Previously Control UI crashed because WS couldn't upgrade through proxy
- Proxy now handles `/mission/*` (static), `/whoop/*` (Flask), everything else → gateway

**Tailscale Serve Fix**
- Tailscale was pointing directly to gateway (bypassing proxy)
- Fixed to point to port 8080 (reverse proxy), which routes correctly

**Control UI**
- Added `allowedOrigins` to gateway config: `https://serenity.tail4695cd.ts.net`, localhost variants
- Origin validation error resolved

**Chaotic Sanctum / Mission Control**
- Added "System Status" button to main page hero-actions (green accent)
- Created `status.html` page showing service health + system metrics
- All links working: Roster, Talos Profile, Ikaris Profile, Sanctum Offices, Devices, System Status

**Default Model**
- Set `agents.defaults.model = "ollama/minimax-m2.7:cloud"` in openclaw.json
- Gateway restart pending to apply (user will restart)

**Cleanup**
- Removed orphaned WhisPlay scripts from `scripts/`
- Killed orphaned `reverse_proxy.py` process, enabled systemd service instead
- Reverse proxy now managed by `reverse-proxy.service`

**vector-memory Fix**
- Fixed `memory_flush.py` to skip gracefully when GEMINI_API_KEY not set (was crashing)
- Now logs warning and reports `total_stored: 0` instead of raising exception

### Current system state
- Gateway: running (needs restart for model change)
- Reverse Proxy: running under systemd
- IT Manager timers: active
- Chaotic Sanctum dashboard: live at `/mission/`
- System Status page: live at `/mission/status.html`
- Tailscale Serve: routing through reverse proxy

### Notes
- Browser cannot fetch localhost HTTP from HTTPS page — status page shows "IT Manager monitors" for local services
- here.now pages (Roster, Talos, Ikaris profiles) are external and working fine
- WhisPlay Pi Zero reflash in progress — starting fresh on that project

### Next
- Restart gateway to apply default model change
- Pi Zero WhisPlay fresh image when ready
- here.now API key available if republishing needed
